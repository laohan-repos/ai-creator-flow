# CreatorFlow 开发规则

## 项目定位

CreatorFlow 是本地运行的书籍短视频生产工具。当前参考实现使用 Next.js 15、React 19、TypeScript 和 Node.js 内置 SQLite，将脚本、图片、配音、素材和剪映草稿串成四步工作流。

## 工作方式

- 修改前先阅读 `docs/CreatorFlow-需求文档.md`、`docs/CreatorFlow-设计稿.md` 和相关代码；一次聚焦一个模块。
- 保留用户已有改动，不重写无关文件。
- 不使用假图片、假音频、假草稿链接伪装成功结果。
- 外部服务未配置或调用失败时，返回明确的中文错误。
- API Key 只允许写入本地环境文件或数据库，不得提交真实 Key，不得回显已保存的 Key。
- 页面文案使用简体中文，代码标识符使用英文。
- 优先复用 `lib/` 中的类型和服务，不在页面内复制服务端逻辑。

## 技术约束

- 使用 Next.js App Router。
- 使用 TypeScript，不新增 `any` 规避类型检查。
- 使用原生 `fetch` 调用外部接口。
- 使用 Node.js 内置 `node:sqlite` 保存本地数据。
- 不引入 UI 框架、ORM、状态管理库或测试框架，除非阶段任务明确要求。
- 服务端错误使用合适 HTTP 状态码，并返回 `{ "error": "中文说明" }`。
- 生成类按钮执行期间必须有加载状态并避免重复提交。

## 验证要求

每个阶段结束前至少执行：

```bash
npm run build
```

文档变更后执行：

```bash
npm run course:check
```

交付时说明：

1. 修改了哪些文件。
2. 完成了哪些验收项。
3. 执行了哪些命令及结果。
4. 仍有哪些限制或需要真实外部服务验证的内容。
