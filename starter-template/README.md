# CreatorFlow Student Starter

这是“用 Codex 构建 CreatorFlow”课程的最小起点，只包含 Next.js、TypeScript 和三个空页面。

开始前，从主项目复制 `AGENTS.md`、`docs/CreatorFlow-需求文档.md` 和 `docs/CreatorFlow-设计稿.png` 到本目录。

```bash
npm install
cp .env.example .env.local
npm run dev
```

参照需求文档中的八个模块逐项实现，并按设计稿完成界面。不要从完整参考答案复制代码。
