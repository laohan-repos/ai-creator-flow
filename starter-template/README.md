# CreatorFlow Student Starter

这是“用 Codex 构建 CreatorFlow”课程的最小起点，只包含 Next.js、TypeScript 和三个空页面。

开始前，从课程包复制 `AGENTS.md`、`docs/CreatorFlow-需求文档.md` 和 `docs/course/` 到本目录。

```bash
npm install
cp .env.example .env.local
npm run dev
```

按照 `docs/course/README.md` 的顺序完成 8 个阶段。不要从完整参考答案复制代码。

