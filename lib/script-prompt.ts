export const DEFAULT_SCRIPT_PROMPT = `用户输入一本书名：{{bookTitle}}

根据主题生成一份适合解读书籍图文视频的完整口播脚本，包括：作者及核心情绪、口播脚本、配图提示词；总时长严格为 24–28 秒。

作者与核心情绪格式示例：陈忠实，中国当代文学；核心情绪：厚重 / 宿命 / 坚守 / 土地。

脚本使用 s0 到 s6 共 7 个独立分镜。一个 s 编号就是一个完整分镜，绝对不能把相邻编号合并：
s0 = 独立口播 + 独立 speed + 独立时长 + 独立配图提示词
s1 = 独立口播 + 独立 speed + 独立时长 + 独立配图提示词
依此类推直到 s6。
画面规则：s0、s1 使用用户选择的片头视频，不生成配图，也不需要 imagePrompt；s2 必须单独生成当前书籍的专属主视觉图；s3–s6 分别生成匹配正文的配图。

七个分镜的职责必须严格遵守：
- s0：根据当前书籍的主题和核心情绪，动态生成一句简短、有吸引力的开场钩子。
- s1：口播固定为“今天我们分享的是”，不得改写，不得加入年代、作者或故事内容。
- s2：口播固定为“《当前书名》。”，只替换书名，不得加入故事内容。
- s3–s6：根据书籍可靠内容生成正文解读、情绪推进和结尾。

s2 的配图表现当前书籍最具代表性的核心意象。不得把正文情节提前写入 s1。

只能输出可确认的事实；不确定的作者、年代和情节写“待核实”，不要编造。

返回严格 JSON，不要 Markdown：
{"bookTitle":"","author":"","mood":"","hook":"","summary":"","scenes":[{"heading":"","narration":"","speed":1.0,"duration":4,"imagePrompt":"中文，竖版9:16，无文字画面提示词"}]}

scenes 数组索引 0–6 分别代表 s0–s6，必须正好为 7 条。每条 narration 都不能为空；s0、s1 的 imagePrompt 必须为空字符串，s2–s6 的 imagePrompt 必须描述当前分镜对应的画面。duration 总和为 24–28，speed 建议为 1.0–1.2。`;
